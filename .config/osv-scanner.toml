# OSV-Scanner exceptions for every lockfile the dependency audit scans.
#
# Only an advisory whose fix lives in an upstream dependency we do not control
# may be listed; anything in our own dependency choices gets fixed instead.
# The Rust entries mirror .cargo/audit.toml, which holds the reasoning, and
# every entry carries an ignoreUntil at most 90 days out: past it the scan
# fails again, so each exception is re-checked against upstream rather than
# accepted forever. build_system/tests/scripts/test_dependency_audit.py holds
# all of this.

[[IgnoredVulns]]
id = "RUSTSEC-2024-0429"
ignoreUntil = 2026-12-15T00:00:00Z
reason = "glib 0.18 VariantStrIter unsoundness via the gtk-rs 0.18 stack Tauri, wry and tray-icon pin on Linux; we never use VariantStrIter"

[[IgnoredVulns]]
id = "RUSTSEC-2024-0370"
ignoreUntil = 2026-12-15T00:00:00Z
reason = "proc-macro-error unmaintained, a build-time macro of glib-macros 0.18 in the same pinned GTK stack; nothing shipped links it"

[[IgnoredVulns]]
id = "RUSTSEC-2025-0081"
ignoreUntil = 2026-12-15T00:00:00Z
reason = "unic-char-property unmaintained, via urlpattern in tauri-utils; no replacement until Tauri moves"

[[IgnoredVulns]]
id = "RUSTSEC-2025-0075"
ignoreUntil = 2026-12-15T00:00:00Z
reason = "unic-char-range unmaintained, via urlpattern in tauri-utils; no replacement until Tauri moves"

[[IgnoredVulns]]
id = "RUSTSEC-2025-0080"
ignoreUntil = 2026-12-15T00:00:00Z
reason = "unic-common unmaintained, via urlpattern in tauri-utils; no replacement until Tauri moves"

[[IgnoredVulns]]
id = "RUSTSEC-2025-0100"
ignoreUntil = 2026-12-15T00:00:00Z
reason = "unic-ucd-ident unmaintained, via urlpattern in tauri-utils; no replacement until Tauri moves"

[[IgnoredVulns]]
id = "RUSTSEC-2025-0098"
ignoreUntil = 2026-12-15T00:00:00Z
reason = "unic-ucd-version unmaintained, via urlpattern in tauri-utils; no replacement until Tauri moves"
