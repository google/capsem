# cargo-fuzz build outputs (libFuzzer-instrumented binaries)
cache/target/
# Crash dumps -- regenerate locally; check in only the minimized
# reproducer when triaging.
artifacts/
# Per-target Cargo.lock is auto-managed by cargo-fuzz.
Cargo.lock
