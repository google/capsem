# Superseded

This draft is not a release contract.

The current serialized, orthogonal release specification is
[`tmp/release-spec.md`](../../tmp/release-spec.md). Implementation state,
dependencies, gates, commits, and changelog entries are tracked in the active
Sprinty ledger at `.sprinty/release-work`.
