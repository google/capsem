# Capsem build system

This project owns Capsem's image builder, release gate, build policies, and
release tooling. Its direct `builder/` source directory installs as the
`capsem_builder` Python package; it intentionally has no intermediate or
nested package-directory compatibility layer.
