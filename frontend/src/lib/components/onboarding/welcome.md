# What's New

- **Air-gapped VM sandboxes** -- run AI agents in isolated Linux VMs
- **MCP tool routing** -- external MCP servers bridged securely into the sandbox
- **Host credential detection** -- API keys and git config auto-detected at setup
- **Security presets** -- medium and high security profiles out of the box
