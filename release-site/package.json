{
  "name": "capsem-release-site",
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "build": "astro build",
    "build:channel": "astro build && node scripts/overlay-dist.mjs dist",
    "check": "tsc --noEmit",
    "dev": "astro dev",
    "preview": "astro preview",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "astro": "^7.1.3",
    "tailwindcss": "^4.3.3"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "@vitest/coverage-v8": "4.1.10",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  },
  "pnpm": {
    "overrides": {
      "js-yaml": "^4.3.1"
    }
  }
}
